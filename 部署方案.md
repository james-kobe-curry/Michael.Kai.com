# 🚀 Michael.Kai.com 部署方案

> ✅ **已上线：https://michael.kai.com/**（2026-08-14）
> 现状：正式部署在公司 AWS 环境，详见文末「实际部署记录」

---

## 实际部署记录（2026-08-14 已完成 ✅）

| 项 | 值 |
|---|---|
| 正式域名 | **michael.kai.com** |
| 服务器 | AWS EC2 香港（`43.198.253.209`，SSH 用户 `ubuntu`，Ubuntu 24.04） |
| 入口架构 | 公司 AWS 负载均衡（ALB）→ 终止 HTTPS + HTTP 301 跳转 → 内网转发到 EC2:80 |
| Web 服务 | Nginx 1.24，站点配置 `/etc/nginx/sites-available/michael` |
| 代码目录 | `/var/www/michael`（git clone，root 属主） |
| 证书 | 由 ALB 托管（无需服务器侧 certbot） |
| 更新方式 | 本地 push GitHub → 服务器 `sudo git pull` |

**更新网站一条命令**：

```bash
ssh ubuntu@43.198.253.209 "cd /var/www/michael && sudo git pull"
```

---

## 一、方案对比总览

| 方案 | 费用 | 上线速度 | 国内访问速度 | HTTPS | 自定义域名 | 备案要求 | 适合场景 |
|---|---|---|---|---|---|---|---|
| **A. GitHub Pages** | 免费 | 5 分钟 | 一般（时快时慢） | ✅ | ✅ | 不需要 | 立刻上线、练手 |
| **B. Vercel / Netlify** | 免费 | 5 分钟 | 一般（部分区域被墙） | ✅ | ✅ | 不需要 | 海外用户为主 |
| **C. Cloudflare Pages** | 免费 | 10 分钟 | 较好 | ✅ | ✅ | 不需要 | 免备案的国内较优解 |
| **D. 云服务器 + Nginx**（阿里云/腾讯云轻量） | 学生机约 ¥10~20/月 | 1~2 天（含备案） | 快 | ✅ | ✅ | **需要 ICP 备案** | 正式对外、投简历 |
| **E. 对象存储 OSS/COS + CDN** | 小站每月几块钱 | 1~2 天（含备案） | 快 | ✅ | ✅ | 需要 | 纯静态站的"标准答案" |

---

## 二、推荐路径

### 第一步（今天就做）：GitHub Pages 免费上线

1. 打开仓库 → **Settings → Pages**
2. Build and deployment：**Deploy from a branch** → `main` / `/(root)` → Save
3. 1~2 分钟后访问：`https://james-kobe-curry.github.io/Michael.Kai.com/`
4. 可选：Settings → Pages → Custom domain 绑定自己的域名（需 DNS 加 CNAME）

> 注意：Pages 上相对路径都能用（你站内都是相对路径 `index.html`、`avatar1.png`，零改动）

### 第二步（正式投简历前）：云服务器 + 自有域名（推荐 D 方案）

适合场景：访问要快、要稳定、简历里放正式域名。

#### 2.1 准备
- **域名**：注册 `kai.com` 家族域名（若已有，直接加子域 `michael.kai.com`）
- **服务器**：腾讯云/阿里云 **轻量应用服务器**，学生优惠约 ¥10~20/月（2核2G 足够）
  - 系统选 **Ubuntu 22.04 / Debian 12**
  - 地域选上海/广州（离你和目标用户近）
- **备案**：国内服务器绑域名**必须 ICP 备案**（在云厂商控制台免费申请，约 1~2 周）——备案下来前可先用 IP 访问测试

#### 2.2 部署步骤（一次配好，以后一条命令更新）

```bash
# 1) 本地（Windows）SSH 登录服务器
ssh root@你的服务器IP

# 2) 服务器上装 Nginx
apt update && apt install -y nginx

# 3) 放网站文件（两种方式任选）
mkdir -p /var/www/michael
# 方式 a：从 GitHub 拉（推荐，以后 git pull 即更新）
git clone https://github.com/james-kobe-curry/Michael.Kai.com.git /var/www/michael
# 方式 b：从本地上传
# 在本地执行：scp -r ./* root@服务器IP:/var/www/michael/

# 4) 配置站点
cat > /etc/nginx/sites-available/michael <<'EOF'
server {
    listen 80;
    server_name michael.kai.com www.michael.kai.com;

    root /var/www/michael;
    index index.html;

    # 简历 PDF 正确下载
    location ~* \.(pdf)$ {
        add_header Content-Disposition attachment;
    }
    # 静态资源缓存（图片/HTML 短缓存）
    location ~* \.(png|jpg|ico)$ { expires 30d; }
    location / { try_files $uri $uri/ =404; }

    gzip on;
    gzip_types text/html text/css application/javascript image/svg+xml;
}
EOF
ln -s /etc/nginx/sites-available/michael /etc/nginx/sites-enabled/
nginx -t && systemctl reload nginx

# 5) 免费 HTTPS（Let's Encrypt）
apt install -y certbot python3-certbot-nginx
certbot --nginx -d michael.kai.com -d www.michael.kai.com
# 自动续期（certbot 默认已装定时任务，验证）：
certbot renew --dry-run

# 6) 云控制台防火墙：放行 80 / 443 端口

# 7) DNS 解析（域名控制台）
# A 记录：michael.kai.com → 服务器IP
```

#### 2.3 以后更新网站

```bash
# 本地改完代码 push 后，服务器上一条命令：
cd /var/www/michael && git pull
```

更进阶：配 **GitHub Actions**，本地一 push 服务器自动 `git pull`（我可以帮你写 `.github/workflows/deploy.yml`）。

#### 2.4 安全与维护
- ✅ HTTPS 强制跳转（certbot 已自动配）
- ✅ 云防火墙只开 22/80/443；SSH 建议改密钥登录、禁 root 密码登录
- ✅ 自动续期证书、`unattended-upgrades` 自动打补丁
- ✅ 监控：UptimeRobot（免费）盯站点可用性，挂了发邮件/微信提醒
- ✅ 备份：代码在 GitHub 就是备份，服务器无需额外备份

---

## 三、成本估算（D 方案）

| 项目 | 费用 |
|---|---|
| 域名 kai.com（已有时为 0） | ~¥60/年 |
| 轻量服务器（学生优惠） | ~¥10~20/月 |
| HTTPS 证书 | 免费 |
| 合计 | **约 ¥180~300/年** |

---

## 四、我的建议

1. **今天**：开 GitHub Pages，先有一个能发给别人的地址（免费、零风险）
2. **投简历/正式对外前**：走 D 方案（域名 + 备案 + 轻量服务器 + Nginx），国内访问快、域名正式（`michael.kai.com` 与你家族域名体系一致）
3. 需要我帮忙时随时说：写 GitHub Actions 自动部署、Nginx 配置调优、甚至帮你把备案/购买流程走一遍

---

*文档生成：2026-08-14 · 对应仓库 github.com/james-kobe-curry/Michael.Kai.com*
